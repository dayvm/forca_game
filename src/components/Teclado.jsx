export default function Teclado({ handleTentativa, letrasCorretas, letrasIncorretas }) {
    return (
        <div>
            <button>A</button>
            <button>B</button>
            <button>C</button>
        </div>
    );
}